// This file is now empty or can be deleted if nothing else uses it.
// The Gemini API interaction logic has been moved to backend API routes (/api/transcribe, /api/translate).

// // Removed code:
// import { GoogleGenerativeAI } from '@google/generative-ai';
// const API_KEY = process.env.NEXT_PUBLIC_GOOGLE_AI_API_KEY || '';
// if (!API_KEY) { ... }
// const genAI = new GoogleGenerativeAI(API_KEY);
// export async function transcribeAudioWithGemini(...) { ... }
// export function isGeminiConfigured(...) { ... } 